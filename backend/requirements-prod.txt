# Production requirements — optimised for 1 GB RAM (Oracle Free Tier VM)
# Key differences from requirements.txt (dev):
#   - Removed: supabase (heavy client, ~150 MB of transitive deps)
#   - Added:   httpx (direct Supabase REST calls — same feature, 10x lighter)
#   - uvicorn without [standard] extras — saves ~15 MB
# Install on VM with:
#   pip install --no-cache-dir -r requirements-prod.txt

fastapi>=0.111.0
uvicorn>=0.29.0
python-dotenv>=1.0.0
groq>=0.9.0
httpx>=0.27.0
pandas>=2.2.0
yfinance>=0.2.40
slowapi>=0.1.9

